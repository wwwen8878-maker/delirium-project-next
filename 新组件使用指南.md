# 🚀 新组件使用指南

## 📦 已创建的组件

### 1. AI对话式评估
**组件路径**：`src/components/ai-conversational-assessment.tsx`

**使用方法**：
```tsx
import { AIConversationalAssessment } from '@/components/ai-conversational-assessment';

// 在页面中使用
<AIConversationalAssessment 
  onComplete={(result) => {
    console.log('评估结果:', result);
    // result包含: score, riskLevel, personalizedAdvice, nextSteps
  }}
/>
```

**推荐放置位置**：
- 首页快速入口
- 独立评估页面（`/assessment-new`）
- 替换现有的复杂问卷

---

### 2. 智能症状追踪
**组件路径**：`src/components/smart-symptom-tracker.tsx`

**使用方法**：
```tsx
import { SmartSymptomTracker } from '@/components/smart-symptom-tracker';

// 在健康日记页面使用
<SmartSymptomTracker />
```

**推荐放置位置**：
- 健康日记页面（替换现有日记）
- 首页快捷入口
- 术后提醒页面

---

### 3. 医护连接模块
**组件路径**：`src/components/medical-team-connection.tsx`

**使用方法**：
```tsx
import { MedicalTeamConnection } from '@/components/medical-team-connection';

// 独立页面使用
<MedicalTeamConnection />
```

**推荐放置位置**：
- 独立的"我的医护"页面（`/medical-team`）
- 首页侧边栏
- 消息通知中心

---

### 4. 优化的AI助手
**组件路径**：`src/components/smart-assistant.tsx`（已优化）

**新功能**：
- 双模式切换（问答/学习）
- 结构化学习模块
- 谵妄专项内容

**使用方法**：
已集成在布局中，右下角浮动按钮自动显示。

---

## 🎨 建议的页面结构

### 方案1：创建新的功能展示页面

创建：`src/app/dashboard/page.tsx`
```tsx
import { AIConversationalAssessment } from '@/components/ai-conversational-assessment';
import { SmartSymptomTracker } from '@/components/smart-symptom-tracker';
import { MedicalTeamConnection } from '@/components/medical-team-connection';

export default function Dashboard() {
  return (
    <div className="container py-12">
      <h1>我的健康管理</h1>
      
      {/* Tab切换或卡片展示 */}
      <Tabs>
        <TabItem title="风险评估">
          <AIConversationalAssessment />
        </TabItem>
        <TabItem title="每日记录">
          <SmartSymptomTracker />
        </TabItem>
        <TabItem title="我的医护">
          <MedicalTeamConnection />
        </TabItem>
      </Tabs>
    </div>
  );
}
```

### 方案2：优化现有页面

**修改首页**（`src/app/page.tsx`）：
```tsx
// 添加快速入口卡片
<div className="grid grid-cols-3 gap-6">
  <Link href="/assessment-new">
    <Card>
      <CardTitle>3分钟风险评估</CardTitle>
      <p>AI对话式评估，快速了解风险</p>
    </Card>
  </Link>
  
  <Link href="/symptom-tracker">
    <Card>
      <CardTitle>每日症状记录</CardTitle>
      <p>1分钟记录，AI智能分析</p>
    </Card>
  </Link>
  
  <Link href="/medical-team">
    <Card>
      <CardTitle>我的医护团队</CardTitle>
      <p>实时连接，双向沟通</p>
    </Card>
  </Link>
</div>
```

---

## 🔗 创建新页面（快速实现）

### 1. AI评估页面
创建：`src/app/assessment-new/page.tsx`
```tsx
import { AIConversationalAssessment } from '@/components/ai-conversational-assessment';

export default function NewAssessmentPage() {
  return (
    <main className="container py-12">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-4xl font-bold mb-8">AI智能评估</h1>
        <p className="text-xl text-gray-600 mb-8">
          3分钟对话式评估，了解您的谵妄风险
        </p>
        <AIConversationalAssessment />
      </div>
    </main>
  );
}
```

### 2. 症状追踪页面
创建：`src/app/symptom-tracker/page.tsx`
```tsx
import { SmartSymptomTracker } from '@/components/smart-symptom-tracker';

export default function SymptomTrackerPage() {
  return (
    <main className="container py-12">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-4xl font-bold mb-8">每日健康记录</h1>
        <p className="text-xl text-gray-600 mb-8">
          每天1分钟，AI帮您监测异常
        </p>
        <SmartSymptomTracker />
      </div>
    </main>
  );
}
```

### 3. 医护团队页面
创建：`src/app/medical-team/page.tsx`
```tsx
import { MedicalTeamConnection } from '@/components/medical-team-connection';

export default function MedicalTeamPage() {
  return (
    <main className="container py-12">
      <div className="max-w-6xl mx-auto">
        <h1 className="text-4xl font-bold mb-8">我的医护团队</h1>
        <p className="text-xl text-gray-600 mb-8">
          实时连接，专业守护
        </p>
        <MedicalTeamConnection />
      </div>
    </main>
  );
}
```

---

## 🎯 快速验证

### 测试步骤：

1. **测试AI评估**：
```bash
# 访问
http://localhost:3000/assessment-new

# 预期
- 看到对话式评估界面
- 点击选项有AI反馈
- 完成后显示结果和建议
```

2. **测试症状追踪**：
```bash
# 访问
http://localhost:3000/symptom-tracker

# 预期
- 看到emoji评分界面
- 提交后AI分析
- 显示风险等级
```

3. **测试医护连接**：
```bash
# 访问
http://localhost:3000/medical-team

# 预期
- 看到医护团队列表
- 显示消息通知
- 数据同步状态
```

4. **测试AI助手**：
```bash
# 任意页面右下角
- 点击浮动按钮
- 切换问答/学习模式
- 查看学习模块
```

---

## 📊 组件特点对比

| 组件 | 旧版 | 新版 | 亮点 |
|------|------|------|------|
| **评估** | 静态问卷 | AI对话 | 3分钟完成 |
| **记录** | 自由文本 | 结构化emoji | AI自动分析 |
| **连接** | ❌ 缺失 | ✅ 完整 | 医患闭环 |
| **助手** | 通用对话 | 谵妄专项 | 学习模式 |

---

## 🎨 UI预览

### AI评估界面
```
┌─────────────────────────┐
│ 🤖 AI: 您的年龄段是？    │
│ 💡 年龄是重要风险因素... │
├─────────────────────────┤
│ [A] <50岁               │
│ [B] 50-64岁             │
│ [C] 65-74岁 ←           │
│ [D] >75岁               │
└─────────────────────────┘
    ↓ 选择后
┌─────────────────────────┐
│ 👤 用户: 65-74岁        │
│ ✅ AI: 已记录，继续...  │
└─────────────────────────┘
```

### 症状追踪界面
```
认知状态
[😊] [🙂] [😐] [😕] [😵]
 5    4    3    2    1

睡眠质量
[😴] [😌] [😑] [😪] [😫]

情绪状态
[😄] [🙂] [😌] [😰] [😤]

[提交] → AI分析中... → 结果
```

### 医护连接界面
```
┌────────────────────┐
│ 我的医护团队       │
├────────────────────┤
│ 👨‍⚕️ 李医生 🟢在线   │
│ 👩‍⚕️ 张护士 🟢在线   │
├────────────────────┤
│ 📬 消息通知 (2条新) │
│ • 张护士: 明天记得...│
│ • 系统: ⚠️ 睡眠异常 │
├────────────────────┤
│ [🚨紧急] [💬咨询]   │
└────────────────────┘
```

---

## 🔧 如果需要帮助

告诉我你想：
1. 创建具体的某个页面
2. 修改组件的某个功能
3. 整合到现有页面中
4. 调整UI样式

我会立即帮你实现！ 🚀

